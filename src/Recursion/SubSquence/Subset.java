package Recursion.SubSquence;

public class Subset {

    // Permutations and combinations
    // subset -> non-adjacent collection, order should be maintained
    // [3, 5, 9] -> [3], [3, 5], [3, 9], [3, 5, 9], [5, 9], [3], [5], [9]
    // Subset Pattern ->  the pattern of taking some element and removing some is known as subset pattern
}
